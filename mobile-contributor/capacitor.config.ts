import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bf.sapsap.contributor',
  appName: 'mobile-contributor',
  webDir: 'www'
};

export default config;
